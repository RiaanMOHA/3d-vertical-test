# toilet-1-f photo → wall index

companion to `room-map.md`. lists which panorama images from each sub-folder show each of the four walls.

⚠ verified 2026-04-29: only `b/` is a true inside-toilet sweep. `a/` is corridor-approach content (corridor-1's bath-wall brick + stairs-wall closet panels) — see "the a/ folder" section below. wall-by-wall coverage is tentative pending full sweep review in phase D.

## wall visibility

### toilet-wall (north — toilet bowl + tank + hand-wash basin + window above)

| current path | image numbers | notes |
|---|---|---|
| toilet-1-f/b/ | b/13 (end of sweep) | toilet bowl + lid in foreground; full toilet-wall view requires full sweep review |

best toilet bowl reference: `toilet-1-f/b/13`. tank + hand-wash basin not directly visible in sampled frames; full sweep review needed.

### brick-wall (east — brick-pattern accent wallpaper, toilet interior)

| current path | image numbers | notes |
|---|---|---|
| toilet-1-f/b/ | b/05, b/13 | toilet's interior brick wall on the camera's right; full ceiling-to-floor brick with white towel ring + small toilet-paper holder mounted on it (b/13) |

best brick-accent reference (toilet interior): `toilet-1-f/b/05` (clean brick), `b/13` (brick with mounted fittings).

note: `toilet-1-f/a/01` shows brick on the left of frame, but that brick is **corridor-1's bath-wall** (east face, seen from the corridor side), NOT the toilet's brick wall.

### door-wall (south — single hinged door from corridor)

| current path | image numbers | notes |
|---|---|---|
| toilet-1-f/b/ | b/01 | white doorframe with chrome hinges visible on right of frame (door closed) |
| toilet-1-f/b/ | b/05, b/13 | door swung open, with corridor's brick wall visible through the opening |

best toilet-entry door reference: `toilet-1-f/b/01` (closed, chrome hinges); `b/13` (open, swing direction visible).

note: the white panel doors with chrome handles in `toilet-1-f/a/01` are **corridor-1 closet panels** (stairs-wall storage doors), NOT the toilet entrance door.

### side-wall (west — plain taupe paint)

| current path | image numbers | notes |
|---|---|---|
| toilet-1-f/b/ | b/01 | taupe textured wall close-up (could be side-wall — orientation TBD) |

best taupe paint / plain wall reference: `toilet-1-f/b/01`.

## what this confirms

- the room's adjacency model (one accent wall + three plain walls + a window above the toilet) is consistent with the `b/` photo evidence — see `b/05`, `b/13`.
- only ONE inside-toilet camera position exists (`b/`); the room is too small for a 4-corner sweep.
- the brick accent is on a long side wall (b/05, b/13); east-vs-west orientation still TBD.
- the toilet door is hinged 開き (single panel with chrome hinges, swings open into corridor), not folding.

## the a/ folder (21 images) — corridor content, NOT inside toilet

verified by content review of `a/01` and `a/10`:
- `a/01`: dark grey-brick wallpaper on left + 4-panel flat closet doors with chrome handles on right + light hardwood floor — wall-to-wall distance much wider than a 0.9 m toilet. this is corridor-1's east face (brick bath-wall) + west face (stairs-wall closet panels).
- `a/10`: white closet panels on left + free-standing taupe column / wall jamb on right + downlight ceiling — also corridor-1.

implication: when sourcing toilet-interior materials, IGNORE `a/`. when reconstructing the corridor-1 scene in phase D, `a/` provides additional reference for corridor brick + closet panels + taupe finishes.

## ceiling and floor

- **ceiling**: flat warm off-white painted finish (visible in `toilet-1-f/b/05`, `b/13`)
- **floor**: light oak plank (visible at the base of toilet bowl in `b/13`)

## file path pattern

each entry corresponds to a file at:

`interior-images/toilet-1-f/<a|b>/toilet-1-f-<a|b>-NN.webp`

example: brick + folding-door reference, toilet-1-f/a photo 1:
`interior-images/toilet-1-f/a/toilet-1-f-a-1.webp`
