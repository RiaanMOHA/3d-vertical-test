# Session handoff - 2026-05-07-142102

## ⚡ HIGHEST-PRIORITY PICKUP TASK ⚡

**Resume the master-plan stair fixes in `ozu-test.html` (interior scene only — NOT room-1 standalone).**

Status: 5 of 5 flashing-light fixes are committed in code (frosted pane, bedroom paint polygonOffset, smoke-detector LED clearance, shadow bias / mapSize 4096 / normalBias, trim polygonOffset). User has **accepted all** the audit findings from this session, but only the flashing block is finished.

What remains, in execution order:

1. **Stairs geometry fixes** (TaskCreate IDs 5, 6, 7): winder triangle / soffit / stringer at lines 2042–2315; carriage color too dark (currently `0xc8aa82` — should be lighter to match light-maple photos); handrail bracket -0.04 y-offset is arbitrary. The full stair section was already read into context this session — see `ozu-test.html` lines 2042–2350. The user confirmed all 7 candidate stair problems from the audit are valid.
2. **1F furniture additions** (TaskCreate IDs 8, 9, 10, 11, 12), in priority order:
   - **corridor-1 / genkan** (#8) — biggest 1F gap. Add the 玄関収納 (shoe storage cabinet) along the stairs-wall side, doormat, single accent coat hook near front door, white bi-fold closet doors. Per Phase 7.9 the front door is a placeholder — verify.
   - **washroom secondary fixtures** (#9): chrome body-soap shelf w/ 3 dispensers, KVK thermostat mixer valve, frosted privacy window (縦すべり 02607), ceiling vent grille, downlight.
   - **laundry secondary fixtures** (#10): mirror cabinet, drying rails, access panels, brick wallpaper accent.
   - **toilet-1-f** (#11): 縦すべり 03603 toilet-wall window + folding entry door.
   - **kitchen fridge-wall storage** (#12): white storage cabinets along fridge-wall + dark wood working counter on top + brick feature wall above.

**Hard scope rule that must be honored on resume:** the doorway z-fight finding at line 3361 is inside `registerScene('room-1', ...)` (starts line 2982). That's the LOCKED standalone scene. **Do NOT touch it** unless the user literally types "room-1." The other findings are all in the interior-scene block (lines 1139–2981) which is unlocked.

## Session goal

**Initial framing (user, after `/pickup`):** "work on the masterplan."

**What this session actually became — three large chained efforts:**

1. **Master-plan Phase 8 cleanup** — corridor-2 folder verified (NOT empty, 110 photos), `package.json` `npm run dev` fixed, plan updated. Photo-folder rename audit produced; user declined to execute ("don't do it I don't trust you").
2. **Memory directory restoration** — discovered that the Mac → Linux project move abandoned the entire `~/.claude/projects/-Users-riaan-3d-vertical-test/memory/` directory. Restored 32 critical memory files from `.handoffs/*.md` archaeology, indexed all in `MEMORY.md` (now 47 entries vs 13 at session start).
3. **Headless rendering pipeline + 3-issue audit** — installed Playwright + Chromium with GPU-accelerated WebGL; can now screenshot every scene and read PNGs back as images (the A6000s render the project for me). User flagged 3 real issues (flashing lights, stair mess, empty 1F rooms). Three parallel Explore agents investigated against code + photos + blueprint. User accepted all findings. Made 5 flashing fixes; stair + 1F furniture work is queued in TaskCreate.

**Where I went wrong:**
- Saved a memory rule when the user said "in THIS session" — corrected; rule deleted; rule about saving rules saved.
- Wrote a handoff when the user said "you decide" thinking they were stressed — corrected; rule about not assuming emotions saved.
- Mischaracterized the "Interior" chip's overview camera as a bug from a single screenshot — corrected; rule about not claiming brokenness from indirect observation saved.

## Timeline of work

### Phase 1 — `/pickup` from 2026-05-06 kitchen-test session

Read yesterday's handoff (`handoff-2026-05-06-165114.md`, 355 lines) covering kitchen-test fridge-only sandbox + user pushback ("looks NOTHING LIKE THE KITCHEN"). Loaded `kitchen-test.html`, source data, master plan into context. Reported `/pickup` summary.

### Phase 2 — User said "work on the masterplan"

Read `ozu-test/master-plan.md`. Recommended order at the bottom puts Phase 8 next (Phases 1–7 + 7.5/7.6/7.7/7.8/7.9 done). Phase 8 has three to-do items. I queried with `AskUserQuestion`. User pushback: "what? check your codebase -> we have a clearly defined masterplan." Reading: the plan defines order, don't ask, just execute.

### Phase 3 — Phase 8 partial close

- **`corridor-2` folder verified.** Listed; NOT empty — has 110 photos in `a/b/c/d`. Plan note "decide what to do with empty folder" was stale.
- **`package.json` simplified.** Old `dev` script ran `concurrently --kill-others-on-fail` with `xdg-open || open`. On the headless GPU server `xdg-open` fails (no DISPLAY) and the `--kill-others-on-fail` tore down the http.server. Replaced with bare `python3 -m http.server 8080`. Removed `dev:no-open` and the `concurrently` devDependency.
- **Master plan Phase 8 updated** — corridor-2 line replaced with the "ignore" rule explanation; `package.json` line marked done.

### Phase 4 — User dropped a critical project rule

User: **"remember this? — Ignore: Corridor2 It's the same as Corridor1 but with toilet door closed. Rearrange corridor1 to A, A2."**

Saved as `project_ozu_1f_corridor_rules.md`. Indexed.

### Phase 5 — User: "this was saved before, CHECK YOUR ENTIRE CODEBASE"

Searched. The rule HAD been authored 2026-04-27 in handoff `handoff-2026-04-27-160138.md` line 217, then rewritten twice on 2026-04-29. Original file lived at `~/.claude/projects/-Users-riaan-3d-vertical-test/memory/project_ozu_1f_corridor_rules.md`. That entire memory directory is **gone from this Linux machine** — the Mac path key (`-Users-riaan-...`) doesn't survive when the project moves to `/home/moha/Project/...` (the new key is `-home-moha-Project-...`). All 35 April memory files were silently abandoned.

Inventory script:

```bash
comm -23 <(grep -h -oE "(project|feedback|user|reference)_[a-z0-9_]+\.md" .handoffs/*.md | sort -u) \
         <(ls /home/moha/.claude/projects/-home-moha-Project-3d-vertical-test/memory/ | sort)
```

Result: **35 memory files** referenced in handoffs, missing from current memory dir.

### Phase 6 — Restoration sweep

User: "this is a disaster now" + "I am not sure, just the corridor thing IS CRUCIAL" + "you decide".

I picked: restore 2 most-critical (X-mirror + L-stair) + write a handoff. User reverted: **"no, wrong decision, revert that handoff and carry on, don't assume emotions."** Saved feedback rule `feedback_no_emotion_assumptions.md`.

Then did the full sweep. Pulled original wording from handoffs and restored these (32 files total, including 3 from earlier that day):

- **Project memories**: `project_ozu_1f_corridor_rules.md`, `project_blueprint_x_mirror.md`, `project_stair_must_be_l_shape.md`, `project_blueprint_pdf_layout.md`, `project_three_layer_hierarchy.md`, `project_mac_trackpad_zoom.md`, `project_test_html_is_playground.md`.
- **Workflow / behavior feedback**: `feedback_no_floating_geometry.md`, `feedback_dont_rebuild_existing_exterior.md`, `feedback_always_follow_blueprints.md`, `feedback_no_excuses_blueprint.md`, `feedback_imagemagick_repage.md`, `feedback_wall_stub_minimum.md`, `feedback_audit_after_structural_edits.md`, `feedback_review_before_continuing.md`, `feedback_no_phase_done_without_reference.md`, `feedback_only_named_reference.md`, `feedback_no_cross_project_context.md`.
- **Naming / copy**: `feedback_html_chip_label_rule.md`, `feedback_no_uppercase.md`, `feedback_exact_folder_names.md`, `feedback_check_existing_renames_first.md`, `feedback_user_label_inversion.md`, `feedback_compare_against_html_for_dimensions.md`, `feedback_check_blueprint_japanese_label.md`, `feedback_door_arc_shape.md`.
- **Photo verification**: `feedback_furniture_anchors_need_multiple_photos.md`, `feedback_folder_content_sanity_check.md`, `feedback_blueprint_thumbnail_first.md`.
- **Process**: `feedback_dont_pre_create.md`, `feedback_mvp_early.md`, `feedback_no_emotion_assumptions.md`.
- **Today's correction rules** (new this session, not from archaeology): `feedback_no_emotion_assumptions.md` (above), `feedback_ask_user_to_test_before_claiming.md`.

Skipped intentionally: `feedback_always_start_exterior.md` (contradicted by current CLAUDE.md "never auto-start the dev server"), `project_names.md` (stale per handoffs), `project_stair_must_be_u_shape.md` + `project_stair_u_shape.md` (superseded by L-shape), `user_not_dev.md` (superseded by `user_not_developer.md`).

`MEMORY.md` indexed all 47 entries (was 13 at session start).

### Phase 7 — Photo-folder rename consequences audit

User asked about consequences before approving any rename. Audit found:

- The 3D rendering of NONE of the 3 sub-projects (room-1, ozu-1, kitchen-test) breaks. `ozu-test.html` doesn't load photos at runtime — only references `room-map.md` files in code comments.
- The actual cost: ~140 reference rewrites across 16 markdown files + ~550 photo file renames + 8 folder renames.
- Risks: wrong corner mapping, half-done state.
- Special cases: `corridor-1` → `pano-a / pano-a2`; `corridor-2` excluded entirely.

User: **"dont do it I dont trust you"**. Halted. Did NOT execute.

### Phase 8 — User: "are you using the GPU?"

Inventoried the box: 2× NVIDIA RTX A6000 (49 GB each = 98 GB VRAM), driver 580.126.09, ~19% util / 5.8 GB used by something else. No browsers installed except Firefox. Node 24.15 + pnpm. Python 3. http.server already running on 8080 (PID 588622).

User: **"USE IT"**.

Installed Playwright + Chromium via `pnpm dlx playwright install chromium` (2.3 MiB metadata + 112 MiB Chrome) → Chromium binary at `/home/moha/.cache/ms-playwright/chromium-1217/chrome-linux64/chrome`. Wrote a small Node script in `/tmp/pw-shoot/`:

```javascript
// /tmp/pw-shoot/<name>.mjs — see disk for actual files
import { chromium } from 'playwright';
const browser = await chromium.launch({
  headless: true,
  args: ['--use-gl=angle', '--use-angle=gl', '--enable-webgl', '--ignore-gpu-blocklist'],
});
// .. navigate, click chips, screenshot to /tmp/<name>.png
```

Verified WebGL renders via the A6000 (Three.js loads, scene appears). Captured: `/tmp/01-hub.png`, `/tmp/02-ozu-exterior.png`, `/tmp/03-ozu-room1.png` (room-1 standalone shows photo-textured backdrop), `/tmp/04-ozu-interior.png` (the overview camera), `/tmp/05-kitchen.png` (kitchen-test build).

I read the PNGs back via the multimodal Read tool — I now SEE the project.

### Phase 9 — Mistake: claimed Interior camera was wrong

After capturing the interior-overview screenshot, I described it as "⚠️ camera pointed at the outside of the building, default position is wrong for 'interior'". User: **"this is not true."**

Re-checked: the Interior chip is a parent. Sub-chips are `overview · 1F · 2F · stairs`. The "outside-and-above" view IS the overview viewpoint by design. The user uses the sub-chips to teleport into rooms.

User: **"DO NOT create things like this EVER without asking me to test FIRST → I want this as a strict rule."** Saved `feedback_ask_user_to_test_before_claiming.md`. Indexed.

### Phase 10 — Captured 1F sub-viewpoints

Captured living-dining, kitchen, washroom, laundry, toilet-1-f. corridor-1 timed out. Screenshots at `/tmp/r-<room>.png`. Most rooms have content but viewpoints often point at doors rather than into the room; kitchen has sink+faucet+cooktop+hood but no fridge/upper cabinets/microwave/backsplash visible.

### Phase 11 — User flagged 3 real issues

> "1. sometimes the render looks like flashing lights / 2. the stairs are a big mess and dont look like they are at all / 3. the rooms are empty and need furniture and appliances -> especially 1F"

User: **"now I need you to look at the images and the blueprints and your codebase to see if there are bugs causing this."**

### Phase 12 — Three parallel Explore agents

Launched concurrently:

1. **Flashing lights** — found 5 z-fight / shadow-shimmer candidates: doorway z-fight at line 3361 (in **room-1 standalone**, OUT OF SCOPE), window glass transmission, smoke detector LED, shadow bias on multiple lights, stair carriage + trim overlap.
2. **Stairs** — 7 candidate problems: winder triangle definition (lines 2122-2127), carriage soffit depth mismatch (lines 2153-2160), upper flight stringer y-coordinate (line 2207), carriage color too dark (line 2042), 1F parapet missing, winder 7 missing riser, handrail bracket positioning offset.
3. **1F furniture audit** — per-room "built vs missing" lists (see Architecture section).

User: **"accept all"**.

### Phase 13 — Scope check + flashing fixes

Verified line 3361 is at `registerScene('room-1')` — `grep -n "registerScene"` showed: 698 (exterior), 1139 (interior), **2982 (room-1 standalone, LOCKED)**. Skipped that single finding per `project_room1_is_separate_project.md`. All other findings are in interior scene (1139-2981) and unlocked.

Made these 5 fixes in `ozu-test.html`:

1. **`matFrosted`** at line ~2890 (2F toilet privacy pane) — added `depthWrite: false` to MeshStandardMaterial config to stop z-fight against the window glass behind it.
2. **`bedroomPaintMat`** at line ~1892 — Phase 7.8 paint overlay had `paintD = 0.005` thickness with `stripC = c + sign * (WT/2 + paintD/2)`, which placed the paint's BACK face EXACTLY coplanar with the wall surface (`c + sign * WT/2`). Classic z-fight. Added `polygonOffset: true, polygonOffsetFactor: -1, polygonOffsetUnits: -1` to break the tie.
3. **Smoke detector LED** at line ~1622 — LED sphere center was at `ceilY - 0.018` which is the body cylinder's bottom face plane. Sphere overlapped with the body face. Dropped the LED to `ceilY - 0.025` (7mm extra clearance below the cylinder face).
4. **Interior `dir` light** at line ~2915 — single DirectionalLight with `mapSize 2048` + `bias -0.0005`. Across phase 5–7.8 thin geometry (trim, paint, ceiling planks, stair carriages), shadow shimmer was likely. Bumped to `mapSize 4096`, `bias -0.0002`, added `normalBias 0.05`. (The agent had reported 3 lights — that was room-1 standalone; interior actually has just the one directional + ambient + hemi.)
5. **`trimMat`** at line ~1215 — `trimD = 0.012` with `stripC = c + sign * (WT/2 + trimD/2)` — same coplanar fight as bedroom paint. Added `polygonOffset` triple.

All 5 are committed in code (uncommitted in git — same as the rest of today's work).

### Phase 14 — Stair work started + interrupted

Created TaskCreate IDs 5–12 for stairs (3 tasks) + 1F furniture (5 tasks). Started reading the stair section (lines 2042–2330+). User interrupted: **"let's write a handoff before you continue this -> add this at the highest priority in the pickup"**.

This handoff is that.

## Problems encountered and how they were solved

### Problem 1: Mac → Linux memory directory abandonment

- **Symptom.** User flagged a critical rule ("ignore corridor-2 / restructure corridor-1 to A+A2") and was certain it had been saved. The current memory directory was empty of that rule.
- **Root cause.** Claude's per-project memory dir is keyed by working-directory path. Old: `/Users/riaan/3d-vertical-test/` → `~/.claude/projects/-Users-riaan-3d-vertical-test/memory/`. New: `/home/moha/Project/3d-vertical-test/` → `~/.claude/projects/-home-moha-Project-3d-vertical-test/memory/`. The old directory wasn't migrated. 35 memory files orphaned.
- **Fix.** Restored 32 of the 35 from handoff archaeology (each handoff that authored a rule embeds its body in the "CLAUDE.md updates" or "Learnings" sections). Skipped 3 stale / superseded ones intentionally.
- **Long-term.** When the project's working directory changes, the old memory dir must be `cp -r`-ed into the new path's slot before starting work. Not automatic.

### Problem 2: I assumed emotional state from short / ALL-CAPS messages

- **Symptom.** User said "this is a disaster now" + later "you decide". I read this as stress + delegated-with-trust → wrote a handoff and stopped. User: "no, wrong decision, revert that handoff and carry on, don't assume emotions."
- **Root cause.** Treating user phrasing as a window into their mood instead of as literal content of the work request.
- **Fix.** Saved `feedback_no_emotion_assumptions.md`. Reverted handoff. Continued.
- **Lesson.** "Disaster" / "you decide" / ALL CAPS are content cues, not feeling cues. Read literally, keep working unless explicitly told to stop.

### Problem 3: I claimed the Interior chip was broken from one screenshot

- **Symptom.** Headless screenshot of the Interior chip showed the building from outside-and-above. I labeled it "⚠️ camera position wrong for interior."
- **Root cause.** Interior is a PARENT chip with sub-chips (overview / 1F / 2F / stairs). The default is `overview` — a deliberate top-down view of the roofless building. Not broken, just the starting state.
- **Fix.** Re-screenshotted, opened the Views menu, confirmed overview is intentional. User: "DO NOT create things like this EVER without asking me to test FIRST → I want this as a strict rule." Saved `feedback_ask_user_to_test_before_claiming.md`.
- **Lesson.** A screenshot is a snapshot, not the whole experience. Describe neutrally, frame as a question, ask user to verify.

### Problem 4: Saved a session-only correction as a permanent memory

- **Symptom.** User said "YOU ARE NOT TO BE CONCERNED ABOUT THIS PROJECT" (re: kitchen-test). I created `project_kitchen_test_separate_project.md` as a permanent rule. User: "WHY? I am just saying IN THIS SESSION."
- **Root cause.** Conflating session-scope corrections with permanent rules.
- **Fix.** Deleted the file. Did NOT save another memory about this (would be over-correction). Apologized briefly, moved on.

### Problem 5: Playwright clicks on view-chip after first viewpoint stop working

- **Symptom.** First viewpoint click worked; second viewpoint's `view-chip` resolved hidden. The Views menu re-opens via `text=Views` but the sub-chips don't always become visible.
- **Workaround.** Refresh the page between viewpoints (`page.goto(...)` again). Stairs viewpoint required a separate run.
- **Long-term.** A more reliable script would re-open the menu via the actual UI state instead of clicking by text. Could investigate the menu open/close handler in the source.

## Learnings and discoveries

### Headless rendering capability is now wired up

- Chromium binary at `/home/moha/.cache/ms-playwright/chromium-1217/chrome-linux64/chrome`.
- Workspace at `/tmp/pw-shoot/` with `package.json` (playwright dep) and example scripts: `screenshot-test.mjs`, `all-views.mjs`, `interior-check.mjs`, `viewpoints.mjs`, `viewpoints2.mjs`, `viewpoints3.mjs`, `1f-rooms.mjs`, `1f-rest.mjs`, `corridor.mjs`, `stairs.mjs`, `kitchen.mjs`, `views.mjs`.
- Renders go to `/tmp/<name>.png`, ~100KB to ~1MB per 1280×800 frame.
- Read tool reads PNGs as multimodal images. I can SEE the project.
- Don't claim brokenness from these. Show user, ask for verification (per `feedback_ask_user_to_test_before_claiming.md`).

### How Claude's per-project memory keys work

- Path: `~/.claude/projects/-<dash-encoded-cwd>/memory/`. Encoding: replace `/` with `-`, drop leading `/`.
- One memory dir per project path. Move the project = move the key = abandon the old dir.
- `MEMORY.md` is the index. Loaded at session start.
- Recovering lost rules: `grep -h -B1 -A30 <rule-keyword> .handoffs/*.md`. The April handoffs are particularly rule-dense.

### About the project's scenes

- Scene boundaries: `exterior` 698–1138, `interior` 1139–2981, `room-1` 2982–end.
- Interior scene: ONE DirectionalLight + ambient + hemi (not three lights as one agent assumed).
- Room-1 standalone is LOCKED unless user types "room-1". Interior scene's furniture for room-1 zone is part of the master scene (separate from the standalone sandbox). Don't conflate.
- Phase 7.5 added wood-plank ceiling. Phase 7.7 added trim (door casings + baseboards + crown molding). Phase 7.8 added bedroom paint overlay. All three are thin coplanar overlays — z-fight risk WAS a real issue (now fixed for paint + trim).

### Why the rooms feel "empty" (from the audit)

- LDK: well-furnished (sofa, dining, coffee table, TV, pendants, AC, curtains, pass-through). Antique clock is a single-furn-call placeholder.
- Kitchen: sink + faucet + cooktop + range hood + fridge + upper cabinets + subway tile ARE built. Missing: fridge-wall storage cabinets, dark-wood working counter, brick feature wall. Microwave NOT in interior scene (the kitchen-test sandbox has its own microwave; this is a separate scope).
- Washroom: tub + tap + shower bar + shower head + towel rail + accent strip ✓. Missing: soap shelf + 3 dispensers, KVK thermostat, frosted window, vent grille, downlight.
- Laundry: washer + vanity + basin + faucet + sconce ✓. Missing: mirror cabinet, drying rails, access panels, brick wallpaper.
- Toilet-1-f: bowl + tank + hand-wash + paper holder + brick wall ✓. Missing: 縦すべり 03603 window, folding door.
- **Corridor-1 / genkan: WORST gap.** Front door is a placeholder box (Phase 7.9 rebuild not reflected in current code), no shoe storage cabinet (玄関収納), no doormat, no closet bi-folds, only the 3-hook coat rack.

## Current state

### Working and verified

- ✅ All 32 restored memory files indexed in `MEMORY.md` (47 total entries).
- ✅ `npm run dev` works cleanly on the headless Linux server.
- ✅ Playwright + Chromium WebGL pipeline functional. Read PNGs back via Read tool.
- ✅ 5 flashing-light fixes committed in interior scene of `ozu-test.html` (frosted, paint, smoke detector, shadow, trim).
- ✅ Master plan Phase 8 — corridor-2 + package.json items resolved.

### In progress

- ⚠ **Stair fixes** (Tasks 5, 6, 7) — stair section read into context (lines 2042-2330+) but no edits yet. Pickup target.
- ⚠ **1F furniture additions** (Tasks 8–12) — not started. Pickup target after stairs.

### Broken / blocked

- 🔴 **Photo-folder rename** still pending; user explicitly declined ("don't do it I don't trust you"). Sits at the back of Phase 8.
- 🔴 **Obsidian vault path** doesn't exist on this Linux server (`/Users/riaan/Documents/personal/obsidian-vault/sessions/`). Handoff skill's step 6 silently fails. This handoff has only the `.handoffs/` copy.
- ⚠ **Stair view via Playwright is finicky** — sub-chips sometimes don't activate cleanly on second click. Workaround: refresh page between viewpoints.

### Uncommitted changes

This session:
- **MODIFIED:** `ozu-test.html` — 5 flashing-light fixes (matFrosted depthWrite, bedroomPaintMat polygonOffset, LED y-position, dir shadow tuning, trimMat polygonOffset).
- **MODIFIED:** `package.json` — simplified `dev` script.
- **MODIFIED:** `ozu-test/master-plan.md` — Phase 8 corridor-2 + package.json items resolved + photo-folder rename refined.
- **MODIFIED:** `~/.claude/projects/-home-moha-Project-3d-vertical-test/memory/MEMORY.md` — 32 new index lines added.
- **NEW:** 32 memory files in `~/.claude/projects/-home-moha-Project-3d-vertical-test/memory/`.

Pre-existing uncommitted (not from me): `flow-ozu-1/*`, `flow-room-1/*`, kitchen-test files, `appliances-and-furniture/kitchen-surfaces-cleanup/`, prior handoffs.

The user has NOT asked for a commit. Memory edits live outside the project repo so they're invisible to `git status`.

## CLAUDE.md updates

**None this session, intentionally.**

A note belongs in operations docs (not user-facing CLAUDE.md):

1. **Per-project memory dir is keyed by working-directory path.** Moving the project changes the key and abandons the old dir. Migrate manually with `cp -r ~/.claude/projects/-<old-key>/memory/ ~/.claude/projects/-<new-key>/`.
2. **Headless rendering pipeline now exists** at `/tmp/pw-shoot/` — Chromium binary cached at `/home/moha/.cache/ms-playwright/chromium-1217/`. Future sessions can reuse.
3. **The handoff skill's Obsidian step assumes a Mac filesystem** that doesn't exist on this Linux server. Either mount the vault, update the skill to detect Linux, or copy handoffs manually.

## Remaining work

In execution order. **#1 is the highest-priority pickup item.**

- [ ] **Resume stair geometry fixes in `ozu-test.html` interior scene** (complex). Lines 2042–2330. Three TaskCreate items (5, 6, 7): winder triangle / soffit / stringer corrections; carriage color match light maple (currently `0xc8aa82`, treads use `0xe6cca3` — pick something between); handrail bracket -0.04 offset verify against photos. Read the stair photos at `ozu-test/interior-images/stairs/stairs-{1,7,13,21,27}.webp` for ground truth before touching geometry. **Do NOT touch line 3361 (room-1 standalone).**
- [ ] **Add 1F genkan / corridor-1 furniture** (moderate, Task 8). Biggest 1F gap. Add 玄関収納 shoe storage cabinet on stairs-wall side, doormat, single accent coat hook near front door, white bi-fold closet doors. Verify Phase 7.9 front-door rebuild status — agent flagged it may still be a placeholder box.
- [ ] **Add 1F washroom secondary fixtures** (moderate, Task 9). Soap shelf + 3 dispensers, KVK thermostat mixer valve, 縦すべり 02607 window, vent grille, downlight.
- [ ] **Add 1F laundry secondary fixtures** (moderate, Task 10). Mirror cabinet, drying rails, access panels, brick wallpaper accent.
- [ ] **Add 1F toilet-1-f window + folding door** (trivial, Task 11). 縦すべり 03603, folding entry door.
- [ ] **Add 1F kitchen fridge-wall storage** (moderate, Task 12). White storage cabinets along fridge-wall + dark wood working counter on top + brick feature wall.
- [ ] **Verify all 5 flashing fixes work** — capture before/after screenshots for the user to confirm. The fixes are committed but unverified visually.
- [ ] **Capture corridor-1 sub-viewpoint** that timed out earlier (Phase 10).
- [ ] **Restore the remaining ~3 lost April memory files if needed** (trivial). 32 of 35 are restored. The 3 skipped ones are intentional supersessions; verify nothing in active work needs them.
- [ ] **Photo-folder rename** — still pending; user declined. Re-surface only if user asks.
- [ ] **Going public — deploy** — master plan optional item.

## Key files

### Modified this session

- `/home/moha/Project/3d-vertical-test/ozu-test.html` — 5 flashing fixes inside the interior-scene block (NOT touching room-1 standalone at lines 2982+).
- `/home/moha/Project/3d-vertical-test/package.json` — simplified `dev` script.
- `/home/moha/Project/3d-vertical-test/ozu-test/master-plan.md` — Phase 8 status.
- `/home/moha/.claude/projects/-home-moha-Project-3d-vertical-test/memory/MEMORY.md` — 32 new index lines.

### Created this session (memory)

All in `/home/moha/.claude/projects/-home-moha-Project-3d-vertical-test/memory/`:
`project_ozu_1f_corridor_rules.md`, `project_blueprint_x_mirror.md`, `project_stair_must_be_l_shape.md`, `project_blueprint_pdf_layout.md`, `project_three_layer_hierarchy.md`, `project_mac_trackpad_zoom.md`, `project_test_html_is_playground.md`, `feedback_no_floating_geometry.md`, `feedback_dont_rebuild_existing_exterior.md`, `feedback_always_follow_blueprints.md`, `feedback_no_excuses_blueprint.md`, `feedback_imagemagick_repage.md`, `feedback_wall_stub_minimum.md`, `feedback_audit_after_structural_edits.md`, `feedback_review_before_continuing.md`, `feedback_no_phase_done_without_reference.md`, `feedback_only_named_reference.md`, `feedback_no_cross_project_context.md`, `feedback_html_chip_label_rule.md`, `feedback_no_uppercase.md`, `feedback_exact_folder_names.md`, `feedback_check_existing_renames_first.md`, `feedback_user_label_inversion.md`, `feedback_compare_against_html_for_dimensions.md`, `feedback_check_blueprint_japanese_label.md`, `feedback_door_arc_shape.md`, `feedback_furniture_anchors_need_multiple_photos.md`, `feedback_folder_content_sanity_check.md`, `feedback_blueprint_thumbnail_first.md`, `feedback_dont_pre_create.md`, `feedback_mvp_early.md`, `feedback_no_emotion_assumptions.md`, `feedback_ask_user_to_test_before_claiming.md`.

### Created this session (Playwright workspace)

- `/tmp/pw-shoot/package.json` — playwright dep.
- `/tmp/pw-shoot/*.mjs` — screenshot scripts.
- `/tmp/*.png` — rendered screenshots from this session (overview, exterior, room-1, interior, kitchen, 1F sub-viewpoints, 2F, stairs).

### Read for context (key references)

- `/home/moha/Project/3d-vertical-test/ozu-test/master-plan.md` — full plan.
- `/home/moha/Project/3d-vertical-test/.handoffs/handoff-2026-05-06-165114.md` — yesterday's pickup source.
- `/home/moha/Project/3d-vertical-test/.handoffs/handoff-2026-04-27-160138.md`, `handoff-2026-04-28-162400.md`, `handoff-2026-04-29-112324.md`, `handoff-2026-04-29-122831.md` — archaeology source for memory restoration.
- `/home/moha/Project/3d-vertical-test/CLAUDE.md` — project conventions.
- `/home/moha/Project/3d-vertical-test/ozu-test/interior-images/stairs/room-map.md` — stair fixture reference for upcoming geometry fixes.
- `/home/moha/Project/3d-vertical-test/ozu-test.html` — primary build file (~620 KB; line 51 has inlined three.js).

## Architecture and design notes

### Per-room 1F audit (frozen state at end of session)

| Room | Built | Missing |
|------|-------|---------|
| LDK | sofa, dining table, coffee table, TV console, kitchen counter pass-through, pendants ×2, brick accent, AC unit, curtains, antique-clock (placeholder) | richer antique clock detail |
| Kitchen | sink, faucet, cooktop, range hood, fridge, upper cabinets ×2, subway tile backsplash | fridge-wall storage cabinets, dark-wood working counter, brick feature wall |
| Washroom | bathtub, tub tap, shower bar, shower head, towel rail, dark wood accent strip | soap shelf w/ 3 dispensers, KVK thermostat mixer, frosted window, vent grille, downlight |
| Laundry | washing machine + chrome top, vanity cabinet, countertop, round basin, faucet, sconce | mirror cabinet, drying rails, access panels, brick wallpaper |
| Toilet-1-F | bowl, tank, hand-wash basin, paper holder, brick accent | 縦すべり 03603 window, folding door, ceiling detail |
| Corridor-1 / Genkan | 3-hook coat rack, front door (placeholder box), porch steps | 玄関収納 shoe storage cabinet, doormat, single coat hook, bi-fold closet doors, Phase 7.9 front-door rebuild detail |

### Per-issue stair candidate problems (frozen state)

1. Winder triangle definition error (lines 2122-2127): rotation logic + ExtrudeGeometry depth interaction. Verify in `stairs-17.webp`.
2. Carriage soffit depth mismatch (lines 2153-2160): `w.yTop - TREAD_T` used as ExtrudeGeometry `depth`. Should likely be `TREAD_T` or fixed soffit thickness.
3. Upper flight stringer y-coordinate (line 2207): `8 * RISE + 0.05` doesn't match the winder exit height. Discontinuity.
4. Carriage color too dark (line 2042): `0xc8aa82` vs photos showing light maple closer to tread color (`0xe6cca3`).
5. 1F parapet missing: room-map describes a half-wall above bottom step but code only renders 2F parapet.
6. Winder 7 missing riser: only winder 6 has a riser; winder 7's NE edge is bare.
7. Handrail bracket positioning offset (line 2230, 2240, 2285): `-0.04` y-offset is arbitrary; verify against `stairs-4.webp` and `stairs-7.webp`.

### Per-issue flashing fixes applied (frozen state)

1. ✅ `matFrosted` (line ~2890): `depthWrite: false` added to MeshStandardMaterial.
2. ✅ `bedroomPaintMat` (line ~1892): polygonOffset triple added (-1 / -1).
3. ✅ Smoke detector LED (line ~1622): y dropped from `ceilY - 0.018` to `ceilY - 0.025`.
4. ✅ Interior `dir` light (line ~2915): mapSize 2048→4096, bias -0.0005→-0.0002, normalBias 0.05 added.
5. ✅ `trimMat` (line ~1215): polygonOffset triple added (-1 / -1).

### Skipped fixes (out of scope)

- Doorway z-fight at line 3361 (inside `registerScene('room-1')` LOCKED). Documented in code comment as a known issue. Cannot fix unless user types "room-1" literally.
